# Handcrafted Hearthwood API — requires Python 3.11+

# Web framework & ASGI server
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# Database: async SQLAlchemy 2.0 + PostgreSQL driver + migrations
sqlalchemy[asyncio]>=2.0.31,<3.0
asyncpg>=0.29,<1.0
alembic>=1.13,<2.0

# Validation & 12-factor settings
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# Multipart form parsing (required by FastAPI for File/UploadFile endpoints)
python-multipart>=0.0.9,<1.0

# Cache & ephemeral shopping carts
redis>=5.0,<7.0

# Object storage (S3 / MinIO), async client
aioboto3>=13.0,<15.0

# OTP delivery via the api.ir gateway (SMS + Call), async HTTP
httpx>=0.27,<1.0

# Secure OTP auth: JWT access tokens
PyJWT>=2.8,<3.0

# Password hashing (reserved for future password-based flows)
passlib>=1.7,<2.0